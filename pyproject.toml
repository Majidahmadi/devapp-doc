[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "DevApp"
authors = [{name = "Majid Ahmadi Jebeli", email = "itpaz@outlook.com"}]
dynamic = ["version", "description"]
